<%= form_with(model: site) do |form| %>
  <% if site.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(site.errors.count, "error") %> prohibited this site from being saved:</h2>

      

      <ul>
        <% site.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :site_name, style: "display: block" %>
    <%= form.text_field :site_name %>
  </div>

  <div>
    <%= form.label :location, style: "display: block" %>
    <%= form.text_field :location %>
  </div>

<div>
  <%= form.label :client_id, "Client", style: "display: block" %>
  <%= form.collection_select :client_id, Client.all, :id, :client_name, { prompt: "เลือก Client" } %>
</div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
